

class Test {
    constructor() {this.attribute = "test";}
}

let test = null;

// for(const index in test) {
//     console.log("[" + index + "]=" + test[index]);
// }

console.log("typeof of object: %s", typeof test==="object");
console.log("instance of object: %s", test instanceof Object);
